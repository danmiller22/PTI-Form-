name: Build & Deploy Frontend to GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'pti-form-deno/frontend/**'
      - '.github/workflows/pages.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install deps
        working-directory: pti-form-deno/frontend
        run: npm install
      - name: Set .env
        working-directory: pti-form-deno/frontend
        run: echo VITE_API_BASE=https://mueller-pti-form-90.deno.dev > .env
      - name: Build
        working-directory: pti-form-deno/frontend
        run: npm run build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pti-form-deno/frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
